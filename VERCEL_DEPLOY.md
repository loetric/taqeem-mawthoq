# دليل النشر على Vercel - خطوة بخطوة

## ✅ الخطوة 1: إعداد GitHub

### أ) إنشاء مستودع جديد على GitHub

1. اذهب إلى: https://github.com
2. اضغط على زر **"+"** في الأعلى → **"New repository"**
3. املأ المعلومات:
   - **Repository name**: `taqeem-mawthoq` (أو أي اسم تريده)
   - **Description**: `منصة تقييم موثوق للأماكن`
   - اختر **Public** (أو Private إذا أردت)
   - **لا** تضع علامة على "Initialize with README"
4. اضغط **"Create repository"**

### ب) رفع الملفات على GitHub

افتح Terminal في مجلد المشروع واكتب:

```bash
# الانتقال لمجلد المشروع
cd /Users/nada/curser-project-test

# تهيئة Git (إذا لم يكن موجوداً)
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit - تقييم موثوق"

# إضافة المستودع البعيد (استبدل YOUR_USERNAME و YOUR_REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# رفع الملفات
git branch -M main
git push -u origin main
```

**ملاحظة**: استبدل:
- `YOUR_USERNAME` → اسم المستخدم في GitHub
- `YOUR_REPO_NAME` → اسم المستودع الذي أنشأته

---

## ✅ الخطوة 2: النشر على Vercel

### أ) إنشاء حساب على Vercel

1. اذهب إلى: https://vercel.com
2. اضغط **"Sign Up"**
3. اختر **"Continue with GitHub"** (الأسهل)
4. سجل دخول بحساب GitHub
5. وافق على الصلاحيات

### ب) ربط المشروع

1. بعد تسجيل الدخول، اضغط **"Add New..."** → **"Project"**
2. ستجد قائمة بجميع مستودعات GitHub
3. ابحث عن المستودع الذي أنشأته (`taqeem-mawthoq`)
4. اضغط **"Import"** بجانب المستودع

### ج) إعدادات النشر

Vercel سيكتشف تلقائياً أنه Next.js! فقط:

1. **Framework Preset**: Next.js (يجب أن يكون محدد تلقائياً)
2. **Root Directory**: `./` (اتركه كما هو)
3. **Build Command**: `npm run build` (يجب أن يكون محدد تلقائياً)
4. **Output Directory**: `.next` (يجب أن يكون محدد تلقائياً)
5. **Install Command**: `npm install` (يجب أن يكون محدد تلقائياً)

**لا تغير أي شيء** - فقط اضغط **"Deploy"**!

### د) انتظار النشر

- سترى شريط تقدم
- سيستغرق 1-3 دقائق
- عند الانتهاء، ستحصل على رابط مثل:
  `https://taqeem-mawthoq.vercel.app`

---

## ✅ الخطوة 3: الوصول من هاتفك

1. انسخ الرابط الذي حصلت عليه
2. افتحه من هاتفك في أي متصفح
3. ✅ المنصة جاهزة!

---

## 🔧 استكشاف الأخطاء

### مشكلة: "Repository not found"
- تأكد أنك رفعت الملفات على GitHub
- تأكد أن المستودع موجود في حسابك

### مشكلة: "Build failed"
- تأكد أن `package.json` موجود
- تأكد أن `next.config.ts` موجود
- راجع رسالة الخطأ في Vercel

### مشكلة: "Cannot find module"
- تأكد أنك رفعت مجلد `node_modules` (لا، لا ترفعه!)
- تأكد أن `package.json` يحتوي على جميع المكتبات

---

## 📱 بعد النشر

### تحديثات تلقائية:
- كل مرة ترفع تغييرات على GitHub، Vercel سينشرها تلقائياً!

### رابط مخصص:
- يمكنك تغيير الرابط من إعدادات المشروع في Vercel

### HTTPS:
- Vercel يوفر HTTPS تلقائياً - آمن 100%!

---

## 🎉 تهانينا!

المنصة الآن متاحة على الإنترنت ويمكن الوصول إليها من أي جهاز!

